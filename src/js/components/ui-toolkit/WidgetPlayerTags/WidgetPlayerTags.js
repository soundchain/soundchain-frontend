import React, { Component } from 'react';
import PropTypes from 'prop-types';

import cx from './WidgetPlayerTags.scss';

export default class WidgetPlayerTags extends Component {

  static propTypes = {
  };

  static defaultProps = {
  };

  render() {
    return (
      <div>WidgetPlayerTags</div>
    );
  }
}