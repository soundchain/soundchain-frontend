import React, { Component } from 'react';
import PropTypes from 'prop-types';

import cx from './WidgetPlayerTop.scss';

export default class WidgetPlayerTop extends Component {

  static propTypes = {
  };

  static defaultProps = {
  };

  render() {
    return (
      <div>WidgetPlayerTop</div>
    );
  }
}