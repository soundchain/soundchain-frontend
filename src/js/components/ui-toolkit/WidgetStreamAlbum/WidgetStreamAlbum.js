import React, { Component } from 'react';
import PropTypes from 'prop-types';

import cx from './WidgetStreamAlbum.scss';

export default class WidgetStreamAlbum extends Component {

  static propTypes = {
  };

  static defaultProps = {
  };

  render() {
    return (
      <div>WidgetStreamAlbum</div>
    );
  }
}