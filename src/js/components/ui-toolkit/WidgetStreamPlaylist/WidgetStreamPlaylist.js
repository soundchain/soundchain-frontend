import React, { Component } from 'react';
import PropTypes from 'prop-types';

import cx from './WidgetStreamPlaylist.scss';

export default class WidgetStreamPlaylist extends Component {

  static propTypes = {
  };

  static defaultProps = {
  };

  render() {
    return (
      <div>WidgetStreamPlaylist</div>
    );
  }
}