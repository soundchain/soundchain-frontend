import React, { Component } from 'react';
import PropTypes from 'prop-types';

import cx from './WidgetStreamTrack.scss';

export default class WidgetStreamTrack extends Component {

  static propTypes = {
  };

  static defaultProps = {
  };

  render() {
    return (
      <div>WidgetStreamTrack</div>
    );
  }
}